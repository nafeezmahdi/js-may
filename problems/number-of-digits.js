let num = 2346167;
// num = 0;

let digits = num.toString().length;
// num.toString() → Converts the number to a string → "2346167".
// .length → Counts the characters in the string → 7.

console.log(digits);
